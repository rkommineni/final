<div>
  <h1> Edit Review</h1>
</div>

<%= form_tag "/reviews/#{@review.id}?book_id=#{@book.id}", :method => 'patch' do%>

  <%= render "form" %>
  <%= submit_tag "Save" %>

<% end %>

<script>
  $("form").validate();
</script>

